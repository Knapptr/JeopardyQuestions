import { StyledHeader,Title } from './styledcomponents/components'

const Header = props =>{
    return(
        <StyledHeader>
            <Title>JEOPARDY!</Title>
        </StyledHeader>)
}

export default Header