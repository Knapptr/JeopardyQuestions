import {MedInfo,StyledQuestion} from './styledcomponents/components'
const Question = ( {question} ) =>{
    return(
        <MedInfo>
        {question}
        </MedInfo>
    )
}

export default Question